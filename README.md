# Gouril

پروژه Next.js 14 (App Router) برای صفحه اصلی گوریل با طراحی مدرن و راست‌به‌چپ. پس از نصب وابستگی‌ها می‌توانید با دستورات زیر توسعه و اجرا کنید:

```bash
npm install
npm run dev
```

صفحه اصلی در `app/page.tsx` پیاده‌سازی شده و استایل کلی در `app/globals.css` قرار دارد.
